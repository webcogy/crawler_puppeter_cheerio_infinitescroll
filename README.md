# 패캠 브런치 크롤러

```
index.js  // 기본 crawler => 크롤링 후 brunch_article.json 생성
brunch.js // puppeteer => 페이지 최하단까지 가는 infinite scroll 예시 (실제 크롤링은 X)
pptr.js   // puppeteer + cheerio => 하나의 페이지 안에 있는 하이퍼링크를 수집하고 재검색
```
